import React, { Component } from 'react';
import { Message } from '../styled-components/components/message';

class MessageComponent extends Component {
  render(){
    return (
      <Message>
        message
      </Message>
    );
  }
}

export default MessageComponent;
